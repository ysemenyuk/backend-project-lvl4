/* eslint-disable no-param-reassign */
export default (app) => {
  // app.statusRepo.findAll = () => {
  // const statuses = await app.objection.models.status.query();
  // return statuses;
  // };
};
